# Maintenance scripts

Place optional helper scripts here, e.g. pruning old experiments or validating required front matter.

Suggested ideas:

- cleanup-experiments: remove `experiments/tmp/` files older than N days
- lint-docs: check for required metadata and headings

