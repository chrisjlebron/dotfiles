{
  "name": "npm-global-installs",
  "version": "1.0.0",
  "description": "XXX: For installing packages globally. Run with npm install -g",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "git-open": "*",
    "ndb": "*",
    "tldr": "*",
    "trash-cli": "*",
    "trymodule": "*"
  }
}
